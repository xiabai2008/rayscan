# WVS v19.2 查漏补缺 — 2026-05-07

## 修复的 Bug

### v19 遗产 Bug（在 v19.2 中同样存在）

| # | Bug | 位置 | 修复 |
|---|-----|------|------|
| 1 | `--modules sqli,xss` 逗号不解析 | `cli.py:109` | 自动 split(",") |
| 2 | 不可达目标 120s 重试风暴 | `crawler.py:162` | 双次连通检查，失败秒退 |
| 3 | Playwright `networkidle` 20s/页 | `crawler.py:711` | 改 `load` + 8s |
| 4 | Playwright TargetClosedError 崩溃 | `crawler.py:708-717` | 全 try/except 包裹 |
| 5 | Auth 失败无提示 | `scanner.py:147` | 加醒目标注 |

### v19.2 专有 Bug

| # | Bug | 位置 | 修复 |
|---|-----|------|------|
| 6 | `is_available()` 缺括号，永远 True | `scanner.py:790,799` | 加 `()` |
| 7 | 版本号显示 v19.0 | `scanner.py:982` | 改 v19.2 |

### Bug 6 详情
`_run_integrations` 中 `self._ffuf.is_available` 和 `self._sqlmap.is_available` 缺少 `()` 调用：
- Python 中方法对象永远 truthy → 即使 ffuf/sqlmap 未安装也尝试执行
- 导致 `FileNotFoundError` 异常
- 修复：`is_available()` 正确调用方法

## 验证结果

```
python -m wvs scan https://about.gitea.com --modules sqli,xss --rate 10 --max-time 120

WVS v19.2 — Web Vulnerability Scanner          ← 版本号正确
Modules: sqli, xss                              ← 逗号解析有效
Crawled 71 pages, discovered 82 endpoints       ← 爬虫正常
发现 0 个漏洞                                   ← 预期（静态站点无漏洞端点）
```

## 未修（非 Bug）
- Lab seed paths 在非 lab 目标返回 403 — 2s 开销可接受
- `js.hs-scripts.com` 外部请求 — 来源不明但只有 1 次
- JSPathfinderDetector / Wappalyzer / ffuf / sqlmap / nuclei 接口无 Bug
